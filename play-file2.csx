int[] nums = { 1, 2, 3, 3, 6, 5, 4, 7, 8, 8, 9 };

object countingObject = new object();

public void countingFunction(arr)
{
    for (int = 0; i < arr.Length; i++)
    {
        if(!object[i])
        {
            countingObject[i] = 1;
        } else
        {
            countingObject[i]++
        }
    }
    Console.WriteLine(countingObject)
}
countingFunction(nums)
// dotnet script play-file2.csx